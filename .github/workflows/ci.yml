name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  format:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Check formatting
        run: npx biome format .

  typecheck:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build highlight dependency
        run: npm run build --workspace=@everton-dgn/polyhive-highlight

      - name: Build relay dependency
        run: npm run build --workspace=@everton-dgn/polyhive-relay

      - name: Build server dependency
        run: npm run build --workspace=@everton-dgn/polyhive-server

      - name: Typecheck all packages
        run: npm run typecheck

  app-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build highlight dependency
        run: npm run build --workspace=@everton-dgn/polyhive-highlight

      - name: Run app unit tests
        run: npm run test --workspace=polyhive-app

  relay-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build relay
        run: npm run build --workspace=@everton-dgn/polyhive-relay

      - name: Run relay tests
        run: npm run test --workspace=@everton-dgn/polyhive-relay

  cli-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Install agent CLIs for provider tests
        run: npm install -g @openai/codex@0.105.0 opencode-ai

      - name: Build highlight dependency
        run: npm run build --workspace=@everton-dgn/polyhive-highlight

      - name: Run CLI tests
        run: npm run test --workspace=polyhive
        env:
          POLYHIVE_LOCAL_SPEECH_AUTO_DOWNLOAD: '0'
          POLYHIVE_DICTATION_ENABLED: '0'
          POLYHIVE_VOICE_MODE_ENABLED: '0'
